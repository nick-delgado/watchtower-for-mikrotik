name: routeros
description: Client for the MikroTik RouterOS API over TLS, with certificate pinning.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0
dependencies:
  args: ^2.7.0
  crypto: ^3.0.7
dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
